<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:padding="@dimen/screen_padding"
    tools:background="@color/black"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <TextView
        android:id="@+id/textViewTitleAsteroid"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/side_margin_recycle_asteroid"
        android:layout_marginTop="@dimen/margin_top_between_titles"
        android:textSize="@dimen/recycle_asteroid_text_size"
        android:textStyle="bold"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Title" />

    <TextView
        android:id="@+id/textViewCloseDateApproach"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="@dimen/margin_top_between_titles"
        android:alpha="0.7"
        android:textSize="@dimen/recycle_asteroid_text_size"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="@+id/textViewTitleAsteroid"
        app:layout_constraintTop_toBottomOf="@+id/textViewTitleAsteroid"
        tools:text="Date"/>

    <ImageView
        android:id="@+id/imgStatusHazardous"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:layout_marginEnd="@dimen/side_margin_recycle_asteroid"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/ic_status_normal"
        android:contentDescription="@string/imgStatusHazardous" />

</androidx.constraintlayout.widget.ConstraintLayout>